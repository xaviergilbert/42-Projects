# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:/usr/local/bin:$PATH

# Path to your oh-my-zsh installation.
export ZSH="/Users/xgilbert/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time oh-my-zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/robbyrussell/oh-my-zsh/wiki/Themes
ZSH_THEME="robbyrussell"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in ~/.oh-my-zsh/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment the following line to disable bi-weekly auto-update checks.
# DISABLE_AUTO_UPDATE="true"

# Uncomment the following line to change how often to auto-update (in days).
# export UPDATE_ZSH_DAYS=13

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# Which plugins would you like to load?
# Standard plugins can be found in ~/.oh-my-zsh/plugins/*
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(
  git
)

source $ZSH/oh-my-zsh.sh

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='mvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch x86_64"

# ssh
# export SSH_KEY_PATH="~/.ssh/rsa_id"

# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
export PATH=$HOME/.brew/bin:$PATH

setopt shwordsplit


alias mesalias="cat ~/.zshrc | grep "alias""
alias ..='cd ../ && ll'
alias config='vim ~/.zshrc'
alias gccw='gcc -Wall -Wextra -Werror'
alias c='clear'
alias 42FileChecker='bash ~/42FileChecker/42FileChecker.sh'
alias update="source ~/.zshrc && ll"
alias cl='c && ll'
alias save='cp ~/.zshrc /Users/xgilbert/xgilbert/GitHub/zshrc.txt && cd /Users/xgilbert/xgilbert/GitHub/ && git add * && git commit -m "save" && git push && ll'
alias gitpull="git pull && cp /Users/xgilbert/xgilbert/GitHub/zshrc.txt ~/.zshrc"
alias 42FileChecker='bash ~/42FileChecker/42FileChecker.sh'


            #alias raccourci dossier github
#aller dans dossier GitHub
alias ggithub="cd /Users/xgilbert/xgilbert/GitHub/ && ll"
#aller dans dossier libft
alias glibft="cd /Users/xgilbert/xgilbert/GitHub/libft && ll"
#aller dans gnl
alias ggnl="cd /Users/xgilbert/xgilbert/GitHub/get_next_line && ll"
#aller dans printf
alias gprintf="cd /Users/xgilbert/xgilbert/GitHub/libft/ft_printf/ && ll"
#aller dans dossier main
alias gmain="cd /Users/xgilbert/xgilbert/GitHub/main && ll"
#aller dans dossier fillit
alias gfillit="cd /Users/xgilbert/xgilbert/GitHub/fillit/ && ll"
#aller dans dossier push_swap
alias gpush="cd /Users/xgilbert/xgilbert/GitHub/push_swap && ll"
#aller dans dossier init
alias ginit="cd /Users/xgilbert/xgilbert/GitHub/init/ && ll"
##aller dans dossier lem_in
alias glem="cd /Users/xgilbert/xgilbert/GitHub/lem_in/ && ll"
##aller dans dossier corewar
alias gcor="cd /Users/xgilbert/xgilbert/corewar/ && ll"

                 #alias import
#import all main in current dir
alias impmain="cp /Users/xgilbert/xgilbert/GitHub/main/printfmain.c . && ll"
#import libft.a dans dossier courant
alias implibft="cp -rf /Users/xgilbert/xgilbert/GitHub/libft . && ll"
#delete libft
alias rmlibft="rm -rf libft/ && ll"
#import de la libft pour push
alias finish="cp /Users/xgilbert/xgilbert/GitHub/auteur . && mkdir libft && cd libft/ && cp -rf /Users/xgilbert/xgilbert/GitHub/libft/ . &&make fclean && cd ../ && ll"
#import des exam test
alias impexam="cd /Users/xgilbert/xgilbert/GitHub/test_exam/ && mkdir `date +%Y%m%d` && cd /Users/xgilbert/exam-basedir && cp -rf * /Users/xgilbert/xgilbert/GitHub/test_exam/`date +%Y%m%d` && cd /Users/xgilbert/xgilbert/GitHub/test_exam/`date +%Y%m%d`/rendu && rm -rf .git && cd ... && ll"

				#test push_swap
#ARG=`ruby -e "puts (0..99).to_a.shuffle.join(' ')"` && ./push_swap $ARG

				#commande utile leaks
#while 1; do leaks nom_du_programme; sleep 1; done

# commande pour que les changements soient pris en compte : source ~/.zshrc
source /Users/xgilbert/.Sublivim/sublivimrc.sh

# 42 settings
USER=xgilbert
export USER
GROUP=staff
export GROUP
MAIL="xgilbert@student.42.fr"
export MAIL
